/// <reference types="node" />
import { REPLServer } from "repl";
export declare class TsRepl {
    private readonly typeScriptService;
    private readonly debuggingEnabled;
    private readonly evalFilename;
    private readonly evalPath;
    private readonly evalData;
    private readonly resetToZero;
    private readonly initialTypeScript;
    private context;
    constructor(tsconfigPath: string, initialTypeScript: string, debuggingEnabled?: boolean, installationDir?: string);
    start(): Promise<REPLServer>;
    private compileAndExecute;
    /**
     * Add user-friendly error handling around compileAndExecute
     */
    private replEval;
    private appendTypeScriptInput;
}
